# Local dataset directory

Place MinerU-produced documents here using:

```text
data/<document>/auto/
    <document>_origin.pdf
    <document>_model.json
```

Do not commit sensitive or institution-restricted source documents unless your project explicitly permits it.
